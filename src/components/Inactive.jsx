import React from "react";
import Button from "./Button";
import { useExtensionContext } from "../Context/ExtentionContex";
import { cards } from "../constants";
const Inactive = () => {
  // const { toggleInActive } = useExtensionContext();
  return (
    <>
    <h1>Inactive data</h1>
    </>
  );
};

export default Inactive;
